{% if wallUser.hasFan(app.user.author) %}
    <a href="{{ path('netizen_famous', {id: wallUser.id, action: 'remove'}) }}" class="button tiny">Unlike</a>
{% else %}
    <a href="{{ path('netizen_famous', {id: wallUser.id, action: 'add'}) }}" class="button tiny">Like</a>
{% endif %}
{% if app.user.isFollowing(wallUser) %}
    <a href="{{ path('netizen_follow', {id: wallUser.id, action: 'remove'}) }}" class="button tiny">Unfollow</a>
{% else %}
    <a href="{{ path('netizen_follow', {id: wallUser.id, action: 'add'}) }}" class="button tiny">Follow</a>
{% endif %}