{# the name is here to prevent a bug from Chrome with the back button #}
<iframe width="640" height="400"
        name="video-{{ content.id }}"
        data-fluid-video
        src="{{ content.url }}"
        frameborder="0">
</iframe>