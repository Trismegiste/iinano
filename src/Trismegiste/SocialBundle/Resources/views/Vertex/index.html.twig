{% extends 'TrismegisteSocialBundle:Default:frontpage.html.twig' %}

{% block content %}
<div class="row-fluid">
    <div class="span6">
        {{ form(form) }}
    </div>
    <div class="span6">
        <ul>
        {% for v in vertex %}
                <li>{{ v.title }} by {{ v.author.nickname }}</li>
        {% endfor %}
        </ul>


    </div>
</div>
{% endblock %}
